apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parish_records_helm.fullname" . }}-test-connection"
  labels:
    {{- include "parish_records_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "parish_records_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
