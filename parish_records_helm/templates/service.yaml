apiVersion: v1
kind: Service
metadata:
  name: {{ include "parish_records_helm.fullname" . }}
  labels:
    {{- include "parish_records_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parish_records_helm.selectorLabels" . | nindent 4 }}
