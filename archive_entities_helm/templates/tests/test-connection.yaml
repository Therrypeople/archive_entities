apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "archive_entities_helm.fullname" . }}-test-connection"
  labels:
    {{- include "archive_entities_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "archive_entities_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
