apiVersion: v1
kind: Service
metadata:
  name: {{ include "archive_entities_helm.fullname" . }}
  labels:
    {{- include "archive_entities_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "archive_entities_helm.selectorLabels" . | nindent 4 }}
