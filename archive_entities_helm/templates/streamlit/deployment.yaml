apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamlit-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: streamlit
  template:
    metadata:
      labels:
        app: streamlit
    spec:
      containers:
      - name: streamlit
        image: your-docker-image:latest
        ports:
        - containerPort: {{.Values.streamlit.port}}
        env: # Need to add other ray and neo4j startup values
        - name: RAY_ADDRESS
          value: "abc"
