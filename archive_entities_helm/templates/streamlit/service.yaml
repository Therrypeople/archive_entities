apiVersion: v1
kind: Service
metadata:
  name: streamlit-service
spec:
  selector:
    app: streamlit
  ports:
    - protocol: TCP
      port: 8080
      targetPort: {{.Values.streamlit.port}}
  type: ClusterIP # Change to ClusterIP if using an Ingress